# ⚡ 快速部署到Vercel（5分钟）

这是一个超级简化的部署指南，让你在5分钟内完成部署。

## 🚀 第一步：准备数据库（2分钟）

### 使用Supabase（推荐，免费且简单）

1. 访问 https://supabase.com
2. 点击 **Start your project** → 使用GitHub登录
3. 点击 **New project**
4. 填写：
   - **Name**: `drone-log-db`
   - **Database Password**: 设置一个强密码（记住它！）
   - **Region**: 选择最近的区域
5. 等待2分钟，数据库创建完成
6. 进入 **Settings** → **Database**
7. 找到 **Connection String** → 选择 **URI** 模式
8. 复制连接字符串，格式类似：
   ```
   postgresql://postgres.xxx:password@xxx.supabase.co:5432/postgres
   ```

✅ 数据库准备完成！

---

## 🔧 第二步：部署到Vercel（3分钟）

### 1. 推送代码到GitHub

打开终端，执行：

```bash
cd "/Users/yang/Downloads/20251113无人机飞行记录APP "

# 初始化Git（如果还没有）
git init
git add .
git commit -m "准备部署"

# 创建GitHub仓库后推送
# （先在 https://github.com/new 创建仓库）
git remote add origin https://github.com/你的用户名/仓库名.git
git branch -M main
git push -u origin main
```

### 2. 导入到Vercel

1. 访问 https://vercel.com
2. 使用GitHub登录
3. 点击 **Add New** → **Project**
4. 选择刚才创建的仓库
5. 点击 **Import**

### 3. 配置环境变量

在配置页面，点击 **Environment Variables**，添加：

**DATABASE_URL**
```
你在第一步复制的Supabase连接字符串
```

**JWT_SECRET**
```
你可以随便输入一个长字符串，例如：
my-super-secret-jwt-key-change-this-12345678
```

### 4. 修改构建命令

在 **Build & Development Settings** 中：

- **Build Command**: 改为
  ```
  npm install && npx prisma generate && npx prisma migrate deploy && npm run build
  ```

- **Output Directory**: `build`

### 5. 部署！

点击 **Deploy** 按钮，等待2-3分钟。

---

## ✅ 第三步：测试（1分钟）

部署完成后，Vercel会给你一个URL，类似：
```
https://your-project.vercel.app
```

### 访问你的应用
1. 打开浏览器，访问上面的URL
2. 你应该能看到登录页面
3. 使用默认账户登录：
   ```
   Email: admin@example.com
   Password: password123
   ```

### 如果看不到页面
可能是数据库还没有初始化，需要手动运行一次数据迁移：

```bash
# 在本地运行（设置数据库URL）
export DATABASE_URL="你的Supabase数据库URL"

# 运行迁移
npx prisma migrate deploy

# 填充初始数据
npx tsx prisma/seed.ts
```

---

## 🐛 遇到问题？

### 问题1：页面空白
**原因**：数据库没有初始化

**解决**：
```bash
# 设置环境变量
export DATABASE_URL="你的数据库URL"

# 运行迁移和种子
npx prisma migrate deploy
npx tsx prisma/seed.ts
```

### 问题2：API请求失败
**原因**：后端没有部署

**解决**：这个项目需要单独部署后端。

**简单方法**：使用Railway部署后端

1. 访问 https://railway.app
2. 使用GitHub登录
3. 点击 **New Project** → **Deploy from GitHub repo**
4. 选择同一个仓库
5. 添加环境变量（同样的DATABASE_URL和JWT_SECRET）
6. 在 **Settings** 中设置：
   - **Start Command**: `npm run backend`
   - **Root Directory**: 留空
7. 部署后会得到一个后端URL，例如：`https://xxx.railway.app`

**然后**修改前端API配置：

创建文件 `.env.production`：
```
VITE_API_URL=https://你的后端URL/api
```

重新部署Vercel项目。

### 问题3：数据库连接失败
**检查**：
1. DATABASE_URL是否正确
2. 在Supabase中检查数据库是否正在运行
3. 确保连接字符串包含 `?sslmode=require`

---

## 🎉 完成！

现在你的应用应该已经运行在：
- 前端：`https://your-project.vercel.app`
- （如果部署了后端）后端：`https://your-backend.railway.app`

---

## 📝 下一步（可选）

### 1. 绑定自定义域名
在Vercel Dashboard → Settings → Domains 中添加你的域名

### 2. 自动部署
现在每次你推送代码到GitHub，Vercel会自动重新部署

### 3. 监控
在Vercel Dashboard查看：
- 访问统计
- 错误日志
- 性能数据

---

**需要更详细的说明？查看完整的 [VERCEL_部署指南.md](./VERCEL_部署指南.md)**

